<div class="col-sm-8 offset-2">
	<%= simple_form_for [@organization, @template] do |f| %>
		<%= f.input :name %>
		
		<%= f.input :body %>

		<%= f.button :submit, class: "btn btn-primary" %>
	<% end %>
</div>